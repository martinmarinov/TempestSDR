This plugin supports ExtIO dll files to be utilized as inputs for TSDR. It takes the location of the ExtIO dll as an command.

You need to compile it with Visual Studio due to ExtIO plugins using Windows MFC therefore no makefile is available.